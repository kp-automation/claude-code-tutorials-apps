# Git Workflow Rules

- Never commit directly to the main branch
- Always create feature branches with descriptive names (e.g. `feat/add-notifications`, `fix/task-status-bug`)
- Run tests before committing (`npm test` for Next.js, `pytest` for FastAPI)
- Write meaningful commit messages — avoid vague messages like "fix" or "update"; describe what changed and why
