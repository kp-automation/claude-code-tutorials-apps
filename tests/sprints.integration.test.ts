/**
 * Sprint integration tests — E2E scenarios exercising the full request/response
 * cycle (real DB, mocked auth). These are stubs to be implemented once the
 * sprint API routes and Prisma schema are in place.
 */

describe("Sprint integration", () => {
  it.todo("Create sprint → appears in list");
  it.todo("Assign task to sprint → task shows in sprint board");
  it.todo("Complete all tasks → sprint can be marked COMPLETED");
  it.todo("Delete sprint → tasks remain (sprint FK nulled)");
});
